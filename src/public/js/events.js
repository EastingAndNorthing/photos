
export function bindEvents() {

    // $(document).on('keydown',function(e) {
    //     // if (e.which == 32 || e.which == 39) // 32 space, 39 arrow right https://keycode.info/
        
    //     // if (e.which == 27 || e.which == 37 || e.which == 81) { // 27 esc, 37 arrow left, 81 q
    //     // }
    // });

    // $(document).on('keyup', function(e) {
    //     if (e.which == 70) // f key
    // })
}
